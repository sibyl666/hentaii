{
  "name": "hentaii",
  "version": "1.0.0",
  "scripts": {
    "dev:all": "npm run dev --workspace=client --workspace=server"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "axios": "^0.26.1",
    "typescript": "^4.6.3"
  }
}
